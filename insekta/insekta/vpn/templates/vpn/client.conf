{% load i18n %}# {% trans "OpenVPN configuration file" %}

# {% trans 'The CAs certificate and your private key' %}
#
ca ca.crt
key client.key

# {% trans 'Your certificates' %}
#{% if certificate_entries %}{% for cert_entry in certificate_entries %}
# Certificate {{ forloop.counter }}
#  * {% trans 'Fingerprint' %}: {{ cert_entry.cert.fingerprint }}
#  * {% trans 'Expire date' %}: {{ cert_entry.cert.expires|date:"SHORT_DATE_FORMAT" }}
{% if not cert_entry.enabled %}# {% endif %}cert {{ cert_entry.filename }}
{% endfor %}
{% else %}
{% blocktrans %}# WARNING: You don't have any certificates. This configuration file will miss
#          the cert entry and therefore it will not work!{% endblocktrans %}
{% endif %}

{% blocktrans %}# More configuration settings. Don't modify them unless you know what you are
# doing.{% endblocktrans %}
client

dev tun
proto udp
remote {{ remote.host }} {{ remote.port }}

resolv-retry infinite
nobind
ns-cert-type server
comp-lzo
verb 3
